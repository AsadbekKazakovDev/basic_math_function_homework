from math import*
def main(a):
    '''find the square root of a number and return it.
    
    Args:
        a (int): a number
        
    Returns:
        float: the absolute value.
    '''
    return sqrt(a)
a = int(input("a = "))
print(main(a))